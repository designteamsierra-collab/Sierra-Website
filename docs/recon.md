# Phase 0 — Reconnaissance Report

**Crawled:** 2026-05-05 · **Source:** sierradigitalinc.com
**Method:** sitemap.xml → REST API metadata (`yoast_head_json`) merged with rendered-HTML body extraction
**Scope:** every URL in `/sitemap.xml` (145 entries: 113 HTML pages + 3 PDFs + 29 stale duplicates that 301 to canonical CPT URLs)

---

## TL;DR (read first)

- **Stack:** WordPress 6.9.4 → Blocksy theme + Blocksy child theme → Elementor Pro 4.0.3 (with Premium Addons + Premium Addons Pro). Caching by FlyingPress, analytics via Google Site Kit (GA4), forms by Contact Form 7, testimonials via "Testimonial Free" plugin.
- **Content volume:** **129 unique canonical URLs** across 6 post types (79 pages, 20 posts, 16 leadership bios, 7 events, 4 webinars, 3 news). Total **~85,700 words** of body copy to migrate verbatim. **1,758 image references**, 100% hosted on origin (no CDN dependency).
- **Three SEO red flags up front** — see [Critical Findings](#critical-findings):
  1. The published sitemap has **16 stale leadership-team URLs** (root-slug) that 301-redirect to their CPT canonical (`/leadership-team/<slug>/`). The new build must preserve **both** the canonical paths _and_ the existing 301s.
  2. **37 pages lack a meta description** — Phase 2 should backfill before launch.
  3. **13 pages have zero inbound internal links** ("orphans") — at risk of deindexing if no one links to them. List below.
- **Third-party tracking is clean:** GA4 + Site Kit + Yoast + Google Fonts + reCAPTCHA (20 form pages) + Vimeo (12 video embeds). **No HubSpot, GTM, Facebook Pixel, LinkedIn Insight, Hotjar, Clarity, Intercom, Drift, or Microsoft Clarity present today.** Brief's Phase 6/7 will install fresh.
- **Performance baseline is blocked:** Google closed the unauthenticated PSI tier (now requires an API key). Need a free key or defer to Phase 1 with local Lighthouse. Recommendation below.

---

## 1. Stack identification

Detected from `https://sierradigitalinc.com/` HTML head + asset paths:

| Layer         | Component                                                     |
| ------------- | ------------------------------------------------------------- |
| CMS           | WordPress **6.9.4**                                           |
| Theme         | **Blocksy** + **Blocksy child theme**                         |
| Page builder  | **Elementor Pro 4.0.3** + Premium Addons + Premium Addons Pro |
| Caching / opt | FlyingPress (`x-flying-press-cache` header observed)          |
| Forms         | Contact Form 7 + `country-phone-field-contact-form-7`         |
| Testimonials  | "Testimonial Free" plugin                                     |
| Analytics     | Google Site Kit 1.176.0 (loads GA4 directly, no GTM)          |
| Anti-spam     | Google reCAPTCHA (on form pages)                              |
| SEO           | Yoast SEO (full `yoast_head_json` exposed via REST)           |
| Hosting / CDN | Cloudflare (with FlyingCDN proxy in front)                    |

**Implication for Phase 2:** Elementor's exported HTML is verbose nested `<div>`s with massive inline styles. We can't take `content.rendered` from REST verbatim — we must parse the rendered HTML, extract the **semantic content** (headings, paragraphs, lists, images, links) word-for-word but discard Elementor's structural cruft, then re-render through our own components.

## 2. URL inventory

### 2.1 By content type (REST canonical view)

| Post type         |   Count | URL pattern                                                                     |
| ----------------- | ------: | ------------------------------------------------------------------------------- |
| `pages`           |      79 | `/<slug>/`                                                                      |
| `posts`           |      20 | `/<slug>/` (no `/blog/` prefix on individual posts)                             |
| `leadership-team` |      16 | `/leadership-team/<slug>/` (canonical) — sitemap lists `/<slug>/` (stale, 301s) |
| `events`          |       7 | `/events/<slug>/`                                                               |
| `webinar`         |       4 | `/webinar/<slug>/`                                                              |
| `news`            |       3 | `/news/<slug>/`                                                                 |
| Archive pages     |       2 | `/blog/2/`, `/blog/3/` (paginated post archive)                                 |
| **Total unique**  | **131** |                                                                                 |

### 2.2 What's in the published sitemap.xml

145 entries · 24 KB · last modified 2025-09-18. Generated by **xml-sitemaps.com** (third-party scraper), **not Yoast** — which is why it has stale entries. Composition:

- 113 unique HTML URLs (16 of which 301-redirect to CPT canonicals)
- 3 PDFs at `/wp-content/uploads/`
- The 113 HTML URLs all returned **200 OK** when crawled (with `-L` redirect follow). No 404s, no 5xx.

### 2.3 Body content stats (HTML-derived)

- Pages with body data: 113
- Total words: **85,721**
- Mean per page: **759** · Median: **640** · Min: 27 · Max: 6,848
- Top 5 longest pages (each is verbatim-protected per the brief — no rewriting):
  - `/terms-of-service/` — 6,848 words
  - `/privacy-policy/` — 4,355 words
  - `/success-stories/` — 2,225 words
  - `/careers/` — 1,683 words
  - `/ecc-to-s4hana/` — 1,476 words
- Mean rendered HTML size: **~1,094 KB** · Max: **1,593 KB**. Heavy. Every page ships ~1 MB of HTML before assets — almost entirely Elementor wrapper divs + inline CSS. **Major perf opportunity for the rebuild.**

### 2.4 Image inventory

- **1,758 `<img>` references** across the 113 pages
- **100% hosted on `sierradigitalinc.com`** (single host, no third-party CDN). All under `/wp-content/uploads/`.
- Mix of `.png`, `.jpg`, `.webp`, `.avif`, `.svg`. Many are pre-resized variants (e.g. `infocus-logo-300x101.png`, `infocus-logo-768x258.png`).
- **Phase 2 plan:** mirror `/wp-content/uploads/**` into `/public/images/wp-content/uploads/**`. Preserve the URLs exactly so any external link to a Sierra image continues to resolve. (We'll regenerate optimized variants via `next/image` at runtime instead of shipping all the WP variants — but the canonical original-resolution image stays at its original path.)

---

## 3. Critical findings

### 3.1 ⚠ Stale sitemap entries for 16 leadership-team pages

**The published sitemap.xml lists each leadership bio at the wrong URL.** Example:

| Sitemap says                                | Yoast canonical                                             | Live behavior                         |
| ------------------------------------------- | ----------------------------------------------------------- | ------------------------------------- |
| `https://sierradigitalinc.com/sandy-udupa/` | `https://sierradigitalinc.com/leadership-team/sandy-udupa/` | 301 → `/leadership-team/sandy-udupa/` |

This applies to all 16 leadership-team pages: `senthil-kumar-ceo`, `maya-bava`, `sudhakar-virupakshi`, `karl-schmidt`, `jayakanth-loganathan`, `m-s-mohanraj`, `tc-venkatesan`, `sameel-baker`, `scott-gabelhart`, `sooriya-sudhakar`, `rajan-marathe`, `kaviarasan-thangarasu`, `venkata-rama-vikas-chinta`, `robert-jones`, `sandy-udupa`, `suchindra-guru-pradeep-prabhakaran`, `narasimman-l-madhavarao` (17 redirects in total — list saved at `/docs/recon-data/recon.json`).

**SEO consequence:** Google has indexed the **canonical** URLs (`/leadership-team/<slug>/`). Backlinks may point at either form. To preserve equity 1:1 we must:

1. Render the new pages at **`/leadership-team/<slug>/`** (matches current canonical)
2. Configure 301 redirects in `next.config.js` from `/<slug>/` → `/leadership-team/<slug>/` for each of the 17 stale slugs
3. Generate the new sitemap.xml from the **canonical** URLs, **not** the legacy sitemap

This is the first decision item I need your approval on before Phase 2.

### 3.2 ⚠ 37 pages missing meta description

The brief mandates 1:1 metadata mirror. For these 37 pages there's nothing to mirror — they have a `<title>` but no `<meta name="description">`. Yoast SEO falls back to an empty/auto-generated description, which Google replaces with a snippet from page content (often suboptimal).

**Recommendation:** during Phase 2, flag these 37 to you as a content task. Either you write descriptions before launch, or we ship without them and accept Google's auto-snippet (same as today). Do **not** auto-generate — that's a copy decision.

### 3.3 ⚠ 13 orphan pages (zero inbound internal links)

These pages exist on the site but nothing on the site links to them:

- `/page-not-found/` — appears to be the 404 template page (expected; not a real orphan)
- `/anxious-about-ai/` (post)
- `/sierra-digital-promotes-premiere-proprietary-btp-readiness-assessments-at-national-sap-conferences/` (post)
- `/events/sierra-digital-showcases-breakthrough-industry-cloud-solutions-at-sap-oil-gas-conference/`
- `/central-finance-consolidation/`
- `/contract-lease-management/`
- `/sap-btp-ai-innovation-day/`
- `/sap-sled-and-hc-team-at-sierra-apphaus/`
- `/sierra-digital-and-sap-bdc/`
- `/the-2040-question-how-to-modernize-sap-bw-without-the-big-bang-risk/`
- `/the-fpa-reconciliation-problem-a-data-black-hole/`
- `/when-the-board-asks-why-solve-your-data-problem-without-complete-transformation/`
- `/your-data-is-more-than-a-system/`

**Question for you:** are any of these already-deleted-but-not-removed-from-sitemap leftovers? If so, we should drop them from the new sitemap. If they're current, we should add them to the relevant index pages or footer.

---

## 4. Schema.org / JSON-LD inventory

Yoast emits a JSON-LD `@graph` block on every page. Across the 129 canonical URLs:

| `@type`          | Pages |
| ---------------- | ----: |
| `WebSite`        |   129 |
| `Organization`   |   129 |
| `BreadcrumbList` |   129 |
| `WebPage`        |   129 |
| `ImageObject`    |   104 |
| `Person`         |    20 |
| `Article`        |    20 |

**Phase 2 plan:** mirror these block-for-block via `generateMetadata()` + a `<JsonLd>` server component. Reuse the values from `yoast_head_json.schema` where possible (modify only the `@id` URLs to match the new canonical structure post-launch). Saved structures are in `/docs/recon-data/recon.json` — we'll feed them into the migration script.

**No `Article` is set on regular `pages` or `events`** today. Phase 2 will preserve that — we don't add types Yoast didn't.

---

## 5. Third-party scripts catalog

Scanned all 113 page HTMLs for known tracking patterns. Results:

| Tracker                      | Pages | Action in rebuild                                                             |
| ---------------------------- | ----: | ----------------------------------------------------------------------------- |
| Google Analytics 4 (gtag.js) |   113 | Reinstall via Phase 7 (need GA4 Measurement ID)                               |
| Google Site Kit              |   113 | **Drop** — Site Kit is WP-only; we'll wire GA4 directly                       |
| Yoast SEO                    |   113 | **Drop** — replaced by Next.js `generateMetadata()` + server-rendered JSON-LD |
| Google Fonts                 |   113 | Keep (or self-host via `next/font` for perf)                                  |
| Google reCAPTCHA             |    20 | Reinstall on contact forms in Phase 6                                         |
| Vimeo                        |    12 | Keep — port `<iframe>` embeds where they appear                               |

**Notably absent today:** GTM, Facebook Pixel, LinkedIn Insight, Hotjar, Microsoft Clarity, HubSpot, Intercom, Drift, Cloudflare Web Analytics, Twitter Pixel.

→ **Phase 6 (HubSpot) and Phase 7 (Clarity) will be net-new installs.** No legacy tracking to migrate or honor cookie-consent for, beyond GA4 and reCAPTCHA.

---

## 6. Hubs / link structure

Top URLs by inbound internal links (i.e. linked from many pages — likely nav/footer items):

| Inbound | URL                                                 |
| ------: | --------------------------------------------------- |
|     113 | `/`                                                 |
|     110 | `/high-tech/`                                       |
|     110 | `/public-sector/`                                   |
|     110 | `/retail/`                                          |
|     110 | `/healthcare/`                                      |
|     110 | `/infocus-readiness-assessment-for-sap-analytics/`  |
|     110 | `/infocus-readiness-assessment-for-sap-datasphere/` |
|     110 | `/hxm360/`                                          |
|     110 | `/infocus/`                                         |
|     110 | `/sap-rise/`                                        |

→ These are the **mega-nav / footer links** that appear on virtually every page. Phase 3 nav component must include them. Full inbound-link map is in `recon.json`.

---

## 7. Performance baseline — BLOCKED

**Status:** unable to run PSI today. Google closed the unauthenticated PageSpeed Insights API tier (`quota_limit_value: 0` for anonymous projects as of recently). Every API call now returns 429.

**Two paths forward — please pick one:**

**Option A — Get a free PSI API key now (90 seconds):**

1. Visit https://console.cloud.google.com/apis/credentials
2. Create a new API key, restrict it to "PageSpeed Insights API"
3. Paste it back to me — I'll run baseline on homepage + 3 inner pages immediately and amend this report

**Option B — Defer to Phase 1:**

- We install `lighthouse` locally as part of the Phase 1 setup
- Run baseline against the live WordPress site, save results to `/docs/baseline-lighthouse/`
- Same data, slightly later. Costs ~10 minutes of Phase 1 setup time.

I'd recommend **Option B** — a local Lighthouse run (Chrome-driven) is generally a more accurate baseline than PSI's lab data anyway, and we'll need Lighthouse locally throughout the project for Phase 8's verification checklist.

**For now: empty file at `/docs/baseline-lighthouse/.placeholder` so the slot is reserved.**

---

## 8. Phase 0 deliverables (what exists on disk)

```
C:\Users\BarathkumarPalanivel\Desktop\Sierra Website\
├── docs\
│   ├── recon.md                       (this report)
│   └── recon-data\
│       ├── sitemap.xml                (raw, 24 KB)
│       ├── urls.txt                   (extracted, 145 entries)
│       ├── _homepage.html             (homepage HTML for stack detection)
│       ├── pages\                     (113 rendered HTML files, ~120 MB total)
│       ├── rest\                      (6 REST JSON files, all post types)
│       ├── psi\                       (4 placeholder 429 responses — pending API key)
│       ├── recon.json                 (master merged data: metadata + body stats per URL)
│       ├── _fetch.sh                  (parallel fetcher, resumable)
│       ├── _extract.ps1               (REST + HTML → recon.json builder)
│       └── _stats.ps1                 (summary stat printer)
└── reference\
    └── sierra_rise_ai_prototype.html  (the gated demo, copied from Downloads)
```

`recon.json` is the canonical machine-readable artifact for Phase 2 migration. Every URL has its `seoTitle`, `seoDescription`, `canonical`, full `schema` JSON, OG/Twitter values, H1, first paragraph, body word count, image count, internal link count, inbound link count, and HTML size.

---

## 9. Open decisions for approval (Phase 0 → Phase 1 gate)

Please confirm or redirect on each:

1. **Leadership-team URL strategy** — render at `/leadership-team/<slug>/` (Yoast canonical) and add 17 `next.config.js` 301 redirects from `/<slug>/`. Confirm?
2. **Orphan page disposition** — review the 13-page list above. Any to drop from the new sitemap, or all to keep?
3. **37 missing meta descriptions** — backfill before launch (you write copy), or ship as-is matching today's gap?
4. **Lighthouse baseline** — Option A (PSI API key) or Option B (local Lighthouse in Phase 1)?
5. **Sitemap source of truth** — generate the new sitemap from the **129 canonical URLs** (excluding stale duplicates), not from the existing 113-URL sitemap?

Once these are settled, Phase 1 (Next.js 15 + Tailwind v4 + pnpm scaffold) is a green light.
